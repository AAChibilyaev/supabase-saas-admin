/**
 * Security Settings Resource
 * Wrapper component for React Admin
 */

import { SecuritySettings } from '../../components/security'

export function SecurityList() {
  return <SecuritySettings />
}
