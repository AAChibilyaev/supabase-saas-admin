VITE_SUPABASE_URL=https://kuxbzqpyesjdhxhnauzs.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_qJ_BnXTFf7WrJGPY3jLcSg_P0O2tmo8

# Typesense Configuration
VITE_TYPESENSE_URL=http://localhost:8108
VITE_TYPESENSE_API_KEY=your-api-key

# Optional: Search-only API key for frontend
# VITE_TYPESENSE_SEARCH_API_KEY=search-only-key

# Optional: Multiple nodes for failover (JSON array format)
# VITE_TYPESENSE_ADDITIONAL_NODES=[{"host":"node2.example.com","port":8108,"protocol":"https"}]

# Stripe Configuration (Issue #28)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Note: Stripe secret keys should be stored in Supabase Edge Functions environment variables:
# - STRIPE_SECRET_KEY (for API calls)
# - STRIPE_WEBHOOK_SECRET (for webhook signature verification)

# Sentry Configuration (Issue #35)
# Get your DSN from https://sentry.io/settings/projects/your-project/keys/
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id

# Sentry Organization and Project (for source map uploads)
VITE_SENTRY_ORG=your-org-name
VITE_SENTRY_PROJECT=supabase-admin

# Sentry Auth Token (for source map uploads)
# Create at: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:releases, project:write
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Sentry Environment (defaults to Vite mode: development/production)
# VITE_SENTRY_ENVIRONMENT=production

# Sentry Release (optional - auto-generated from git commit if not set)
# VITE_SENTRY_RELEASE=1.0.0

# Sentry Performance Monitoring
# Percentage of transactions to send to Sentry (0.0 to 1.0)
# 1.0 = 100% of transactions (use lower values in production)
VITE_SENTRY_TRACES_SAMPLE_RATE=1.0

# Sentry Session Replay
# Percentage of normal sessions to record (0.0 to 1.0)
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1

# Percentage of error sessions to record (0.0 to 1.0)
VITE_SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=1.0

# OpenAI Configuration (Issue #31)
VITE_OPENAI_API_KEY=sk-your-openai-api-key

# Note: For production, OpenAI API key should also be stored in Supabase Edge Functions environment variables:
# - OPENAI_API_KEY (for embedding generation in Edge Functions)
#
# Supported models:
# - text-embedding-3-small (1536 dimensions, $0.02/1M tokens) - Recommended for most use cases
# - text-embedding-3-large (3072 dimensions, $0.13/1M tokens) - Higher accuracy
# - text-embedding-ada-002 (1536 dimensions, $0.10/1M tokens) - Legacy model

# Resend Email Service Configuration (Issue #37)
# Get your API key from https://resend.com/api-keys
VITE_RESEND_API_KEY=re_your_resend_api_key

# Email From Addresses (optional - defaults provided)
VITE_EMAIL_FROM=noreply@yourdomain.com
VITE_EMAIL_FROM_ALERTS=alerts@yourdomain.com
VITE_EMAIL_FROM_NOTIFICATIONS=notifications@yourdomain.com

# Note: For production:
# 1. Verify your sending domain in Resend dashboard
# 2. Configure DNS records (SPF, DKIM, DMARC)
# 3. Set up webhooks for delivery tracking
# 4. Use production-ready email addresses